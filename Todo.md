# Todo List

- [ ] list
  - [ ] nested lists (`<ol>` and `<ul>`) markers
- [ ] code blocks
  - [ ] show line numbers in the code blocks (use `<ol>` in the code blocks?)
  - [ ] when in pdf (`@media print`?), auto newline in the code blocks
- [ ] Figure and table labels
  - [ ] `.figure` and `.table` classes (for `<h6>`?)
